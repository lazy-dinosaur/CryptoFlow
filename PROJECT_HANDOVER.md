# 📦 CryptoFlow Project Handover (Backup & Instructions)

**Date**: January 1, 2026
**Project**: Institutional Footprint Chart (Bookmap/ATAS Style)
**State**: Production Ready (Visuals Tuned)

---

## 1. 🔑 VPS & Infrastructure Details
Save these details securely. This is where the backend lives.

| Parameter | Value |
| :--- | :--- |
| **IP Address** | `100.86.66.124` (Exclude from Public GitHub) |
| **OS** | Windows Server |
| **Username** | `Administrator` |
| **Project Path** | `C:\CryptoFlow` |
| **Access Method** | SSH Key (`~/.ssh/id_ed25519`) or RDP |
| **Database** | SQLite (`server/candles.db`) |
| **Process Manager** | PM2 (Manage Node.js processes) |

### Important Server Commands (via SSH)
```powershell
# Connect to Server
ssh Administrator@100.86.66.124

# Check Status
pm2 status

# Restart Server (Applies backend changes)
pm2 restart all

# View Logs (Debug)
pm2 logs
```

---

## 2. 🚀 Quick Start (Local Development)
Instructions for you or another AI to run the code on a laptop.

1.  **Install Dependencies**
    ```bash
    npm install
    ```
2.  **Start Dev Server**
    ```bash
    npm run dev
    ```
    *Opens local version at http://localhost:5173*
3.  **Build for Production**
    ```bash
    npm run build
    ```
    *Creates `dist/` folder.*

---

## 3. 🚢 Deployment Guide
How to update the live server after making changes.

### Step 1: Build Local
```bash
npm run build
```

### Step 2: Upload to VPS
Use this exact command to upload the new `dist` folder:
```bash
scp -r dist/* Administrator@100.86.66.124:"C:/CryptoFlow/dist/"
```

*Note: The server serves files directly from `C:\CryptoFlow\dist`. No server restart is needed for frontend-only changes (Visuals, Colors).*

---

## 4. 🤖 AI Context Prompt (For Future Edits)
**Copy and paste the text below** into any new AI session to give it instant "expert context" about this project.

> **[BEGIN PROMPT]**
> I am working on "CryptoFlow", a high-performance Canvas-based Footprint Chart for trading.
>
> **Stack**:
> - **Frontend**: Vite + Vanilla JS (No Frameworks). Main logic in `src/components/FootprintChart.js`.
> - **Backend**: Node.js + Express + Better-SQLite3 (`server/api.js`).
> - **Data**: Binance WebSocket + Custom `DepthCollector` for Heatmap.
>
> **Key Visual Features (Do Not Break):**
> 1. **Bookmap Heatmap**: A specialized "Heat Gradient" (Blue->Cyan->Green->Yellow->Red->White) rendering market depth. It is **Time-Aligned** (syncs to candle timestamps), not index-based.
> 2. **3D Bubbles**: Big trades are rendered as 3D spheres with radial gradients (Green/Red), not flat circles.
> 3. **Smart Axis**: X-Axis shows Time + Date (TradingView style) stacked.
> 4. **Zoom Logic**: Allows deep squeeze (`minZoomX: 0.5`).
>
> **Current Task**: [Insert your new request here]
> **[END PROMPT]**

---

## 5. 📂 Project Structure
- **/src/components/FootprintChart.js**: **THE CORE**. 90% of the visual logic (Canvas, Rendering, Zoom, Heatmap) is here.
- **/src/main.js**: App controller, WebSocket connection, Data Aggregation.
- **/server/depth-collector.js**: Captures order book history for the heatmap.
- **/server/api.js**: The API server.

## 6. How to Backup Now
To back up everything to your local drive:
1.  Go to `Documents` on your Mac.
2.  Find the folder `ATAS 2`.
3.  **Right Click -> Compress "ATAS 2"**.
4.  Save the `.zip` file to a USB stick or Cloud.

---
*Generated by Antigravity Agent*
