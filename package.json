{
  "name": "crypto-orderflow",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && echo 'Run: git push && ssh vps cd ~/projects/CryptoFlow && git pull'"
  },
  "dependencies": {
    "lightweight-charts": "^4.1.3"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
